@model IEnumerable<IRRM.Models.RiskAssessors_T>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RiskID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users_M.UserName)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RiskID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Users_M.UserName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.RiskAssessorID }) |
                @Html.ActionLink("Details", "Details", new { id=item.RiskAssessorID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.RiskAssessorID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
