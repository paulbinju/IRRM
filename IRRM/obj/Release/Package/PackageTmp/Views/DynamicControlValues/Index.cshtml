@model IEnumerable<IRRM.Models.DynamicControlValue>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ControlValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DynamicControl.ControlName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DynamicControlType.DControlType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ControlValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DynamicControl.ControlName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DynamicControlType.DControlType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ControlValueID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ControlValueID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ControlValueID })
        </td>
    </tr>
}

</table>
