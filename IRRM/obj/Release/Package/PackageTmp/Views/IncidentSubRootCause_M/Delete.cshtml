@model IRRM.Models.IncidentSubRootCause_M

@{
    Layout = null;
}

<style>
    .dl-horizontal dt {
        width: 250px;
        margin-right: 15px;
    }
</style>
<h3>Are you sure you want to delete this?</h3>
<div>

    <dl class="dl-horizontal">

        <dt>
            Incident Main Root Cause
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IncidentMainRootCause_M.IncidentMainRootCause)
        </dd>
        <dt>
            Incident Sub Root Cause
        </dt>
        <dd>
            @Html.DisplayFor(model => model.IncidentSubRootCause)
        </dd>


 

    </dl>


    @using (Html.BeginForm(null, null, FormMethod.Post, new { id = "delform-" + Html.DisplayFor(model => model.IncidentSubRootCauseID) }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger col-md-offset-1" />
        </div>
    }


</div>

<script>
    $(document).ready(function () {
        $("#delform-"+@Html.DisplayFor(model => model.IncidentSubRootCauseID)).submit(function (e) {
            e.preventDefault();
            var formdata = new FormData(this);

          //  alert($("#DFormID").val());
            $.ajax({
                method: "POST",
                url: "@Url.Content("~/IncidentSubRootCause_M/Delete/" + Html.DisplayFor(model => model.IncidentSubRootCauseID))",
                data: formdata,
                mimeTypes: "multipart/form-data",
                contentType: false,
                cache: false,
                processData: false,
                success: function (data) {
                   // alert('Successfully deleted!');
                    window.location.href = "@Url.Content("~/IncidentSubRootCause_M")";
                },
                error: function () {
                    alert('Unable to delete, try editing instead! ');
                }
            });
        });
    });
</script>