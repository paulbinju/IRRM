@model IEnumerable<IRRM.Models.DynamicControlType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DControlType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HasValues)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DControlType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HasValues)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DControlID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DControlID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DControlID })
        </td>
    </tr>
}

</table>
