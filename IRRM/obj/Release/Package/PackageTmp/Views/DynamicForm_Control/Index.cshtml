@model IEnumerable<IRRM.Models.DynamicForm_Control>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DFormID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DControlID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DFormID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DControlID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DFormControlID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DFormControlID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DFormControlID })
        </td>
    </tr>
}

</table>
